# یادداشت انتقال پروژه (برای Claude بعدی)

این فایل را قبل از هر تغییری در پروژه بخوان.

## آخرین تحویل: درخواست خودکار Permission موقعیت + بازطراحی FAB به منوی دو-گزینه‌ای

دو تسک جدا در یک پرامپت:

**۱) موقعیت/GPS هیچ‌وقت واقعاً درخواست نمی‌شد.** کاربر گزارش داد اپ در نصب اصلاً اجازه لوکیشن نمی‌خواهد، پس گمان می‌کرد قابلیت GPS الکی گذاشته شده. بررسی کد نشان داد `locationService.ts` (مرکزی، native-first روی `@capacitor/geolocation`) از قبل کاملاً درست بود؛ مشکل واقعی این بود که تنها نقطه‌ی صدازدنش در `OnboardingFlow.tsx` (مرحله ۲) پشت یک دکمه‌ی کوچک اختیاری «استفاده از موقعیت دقیق من» بود که هیچ‌کس مجبور به زدنش نبود — یعنی پرامپت بومی Permission اندروید عملاً هیچ‌وقت ظاهر نمی‌شد مگر کاربر خودش آن دکمه‌ی کم‌دیده را پیدا و لمس می‌کرد. **رفع:** یک `useEffect` (با `useRef` برای یک‌بار-اجرا) در `OnboardingFlow.tsx` اضافه شد که به‌محض رسیدن به مرحله ۲ (همان مرحله‌ای که توضیح «شهر فقط برای آب‌وهوا» را نشان می‌دهد)، خودش `requestLocation()` را صدا می‌زند. چون `ensurePermission()` داخل `locationService.ts` از قبل وضعیت فعلی را چک می‌کند، اگر کاربر قبلاً اجازه داده یا دائمی رد کرده، دوباره مزاحمش نمی‌شود. دکمه‌ی دستی برای تلاش دوباره (مثلاً بعد از رد اولیه) دست‌نخورده باقی ماند. هیچ منطق دیگری در `locationService.ts`/`weatherService.ts` تغییر نکرد چون از قبل درست بودند (رفتار آفلاین‌فرست با `getLastKnownLocation` روی Home هم دست‌نخورده ماند — آنجا عمداً خودکار نیست تا هر بار باز کردن اپ مزاحم نشود).

⚠️ **محدودیت مهم:** این فقط کاربرانی را پوشش می‌دهد که از این به بعد Onboarding را طی می‌کنند (نصب تازه، یا `onboardingCompleted` هنوز `false`). کاربرانی که قبلاً Onboarding را رد کرده‌اند، با این تغییر دوباره پرامپت نمی‌بینند مگر خودشان دکمه‌ی دستی در همان صفحه یا در کارت آب‌وهوای Home را بزنند — اگر کاربر می‌خواهد این گروه را هم پوشش دهد (مثلاً یک درخواست خودکار یک‌باره در اولین بازدید Home بعد از آپدیت، برای کسانی که هرگز درخواست نشده)، باید جداگانه با پرچم `roza_location_permission_requested_v1` در `App.tsx` اضافه شود — در این تحویل عمداً اضافه نشد چون پرامپت کاربر فقط به «نصب» اشاره داشت.

**۲) بازطراحی FAB به Floating Action Menu.** قبلاً `BottomNavigation.tsx` یک FAB با `onFabClick` تکی داشت که در `App.tsx` مستقیماً `setActiveSection('personalRoutine')` صدا می‌زد — یعنی مسیر «افزودن محصول» (`activeSection: 'products'`) از این دکمه اصلاً در دسترس نبود. **رفع:** `BottomNavigation.tsx` بازنویسی شد تا به‌جای `onFabClick`/`fabLabel`، یک prop جدید `fabActions: FabAction[]` بگیرد. اولین تپ روی FAB حالا یک منوی دو-گزینه‌ای شیشه‌ای (Glassmorphism هماهنگ با بقیه اپ) باز می‌کند: «🧴 محصول پوستی» (آیکون `FlaskConical`) و «✨ به روتین پوستی» (آیکون `Sparkles`) — بدون ایموجی واقعی در UI، فقط آیکون‌های lucide. انیمیشن با `motion/react`: `AnimatePresence` + `staggerChildren` برای ظاهرشدن مرحله‌ای گزینه‌ها (scale+fade+translateY، همان easing `[0.16,1,0.3,1]` که `DrawerMenu.tsx` هم استفاده می‌کند)، خود دکمه FAB با `rotate: 135` به شکل X در می‌آید، بک‌دراپ نیمه‌شفاف برای بستن با تپ بیرون. `App.tsx` هم به‌روزرسانی شد: `handleBottomNavFabClick` حذف و با یک `fabActions` (از طریق `useMemo`) جایگزین شد که دقیقاً همان دو مسیر قبلی (`'products'` و `'personalRoutine'`) را صدا می‌زند — خودِ `ProductShelf.tsx` و `PersonalRoutineView.tsx` هیچ تغییری نکردند، فقط مسیر ورود.

**فایل‌های تغییریافته:**
- `src/components/onboarding/OnboardingFlow.tsx` — `useEffect` خودکار برای درخواست Permission در مرحله ۲.
- `src/components/layout/BottomNavigation.tsx` — بازنویسی کامل: `FabAction`/`fabActions`، منطق باز/بسته منو، انیمیشن.
- `src/App.tsx` — `handleBottomNavFabClick` حذف، `fabActions` جایگزین شد؛ ایمپورت‌ها به‌روزرسانی شد.

**تست انجام‌شده:** این محیط نه `node_modules` کامل دارد (نصب npm با خطای ۴۰۳ از رجیستری شکست خورد — بدون دسترسی شبکه) نه دستگاه/شبیه‌ساز اندروید، پس نه `tsc` کامل روی کل پروژه قابل اجرا بود نه تست رفتار واقعی روی گوشی. کد به‌صورت دستی و دقیق بازبینی شد (تطبیق JSX/براکت‌ها، نوع Propها، مسیر ایمپورت‌ها). **توصیه‌ی صریح: قبل از هرگونه اعلام قطعیت به کاربر نهایی، حتماً `npm install` واقعی (با دسترسی شبکه)، `npm run dev` یا `npm run android:build` را اجرا کن و روی حداقل یک گوشی/امولاتور اندروید واقعی تست کن که:**
1. در اولین اجرای اپ بعد از نصب تازه، رسیدن به مرحله ۲ Onboarding واقعاً پرامپت بومی Permission موقعیت اندروید را نشان می‌دهد.
2. با رد کردن پرامپت، پیام خطای فارسی مناسب نمایش داده می‌شود و اپ کرش نمی‌کند.
3. تپ روی FAB منو را با انیمیشن باز می‌کند، تپ روی هرکدام از دو گزینه دقیقاً همان صفحه‌ی قبلی (ProductShelf / PersonalRoutineView) را باز می‌کند، تپ بیرون یا تپ دوباره‌ی FAB منو را می‌بندد.

## تحویل قبلی: Audit کامل سیستم اعلان‌ها + رفع گپ «resync فوری»

کاربر خواست یک ممیزی کامل سیستم Notification انجام شود تا رفتار native-first، قابل‌اعتماد، مستقل از WebView داشته باشد (طبق چک‌لیست هفت‌مرحله‌ای پرامپت کاربر). نتیجه ممیزی: معماری فعلی `notificationService.ts` (از تحویل‌های قبلی) از قبل دقیقاً روی همان اصولی ساخته شده بود که تسک می‌خواست — تاریخ مطلق به‌جای `on+repeats`، `withTimeout` دور هر تماس پلاگین، افق چرخشی ۱۰روزه، مدیریت فعال مجوز Exact Alarm، ID پایدار per-entity. **هیچ بازنویسی معماری لازم نبود.**

تنها گپ واقعی پیدا‌شده: نوبت‌ها (`Appointment`) و ثبت/حذف پریود (`PeriodLog`) مستقیم روی `LocalDB` نوشته می‌شوند، نه روی `userState` — و افکت زمان‌بندی در `App.tsx` فقط به تغییر مشخصی از کلیدهای `userState` (و رویداد `resume`) گوش می‌داد. یعنی اگر کاربر، مثلاً، یک نوبت را همان لحظه که اپ باز است لغو می‌کرد، اعلان همان نوبت تا دفعه بعدی که اپ resume می‌شد (یا یکی از آن کلیدهای خاص عوض می‌شد) هنوز روی گوشی زمان‌بندی‌شده می‌ماند و نمایش داده می‌شد.

**رفع:** یک تابع مشترک `resyncNotifications` در `App.tsx` (بر پایه همان `scheduleRozaNotifications` موجود) به هر دو مسیر مصرف‌کننده وصل شد:
- `src/components/appointments/AppointmentsView.tsx` — prop اختیاری جدید `onAppointmentsChanged`؛ بعد از ایجاد نوبت، «انجام شد»، و «لغو» صدا زده می‌شود.
- `src/components/cycle/CycleDashboard.tsx` — prop اختیاری جدید `onCycleDataChanged`؛ بعد از ثبت پریود دستی (`confirmManualPeriod`) و هر دو مسیر حذف پریود (باز/تاریخچه) صدا زده می‌شود.

هیچ منطق دیگری در `notificationService.ts` تغییر نکرد — چون از قبل درست بود. هیچ فایل حذف یا معماری از نو بازنویسی نشد.

**تست انجام‌شده:** چون این محیط نه `node_modules` دارد (npm registry مسدود بود، `npm ci` با خطای 403 شکست خورد) نه دستگاه/شبیه‌ساز، نمی‌شد runtime واقعی تست کرد. `tsc --noEmit` مستقل (با `--skipLibCheck` برای دورزدن نبود type declarations نصب‌نشده) روی کل پروژه اجرا و خروجی قبل/بعد از تغییرات مقایسه شد — **هیچ خطای نوع جدیدی از این ویرایش‌ها اضافه نشد** (فقط همان خطاهای preexisting ناشی از نبود `node_modules`، که در تحویل‌های قبلی هم گزارش شده بودند). با خواندن دقیق `getUpcomingAppointments` (فیلتر `canceled`/`done` از قبل درست بود) و `getTodayCycleState`/`computeCycleState` (همیشه از `LocalDB.getPeriodLogs()` تازه می‌خواند)، تأیید شد که تنها چیزی که کم بود «صدا زدن دوباره‌ی `scheduleRozaNotifications`» بود، نه منطق پیش‌بینی.

**چیزی که نمی‌شد اینجا تست کرد (نیاز به گوشی واقعی):** تمام ۱۳ سناریوی تست خواسته‌شده در پرامپت کاربر (بستن اپ، ری‌استارت گوشی، Doze/Battery Saver، اندروید ۱۳+، نبود اینترنت، و غیره) نیاز به یک دستگاه اندروید واقعی یا حداقل امولاتور دارند که این محیط ندارد. همچنین این پروژه هنوز پوشه `android/` ندارد (`npx cap add android` اجرا نشده)، پس هیچ‌کدام از موارد سطح Native (AndroidManifest واقعی، Gradle، boot receiver، Doze/battery‌optimization exemption) در این محیط قابل بازرسی مستقیم نبودند — این دقیقاً محدودیت گزارش‌شده در تحویل‌های قبلی هم بود. توصیه: بعد از `npx cap add android` و `npm run cap:sync`، حتماً روی یک گوشی اندروید واقعی (ترجیحاً ۱۲+) تمام ۱۳ سناریو را تست کن، مخصوصاً لغو نوبت/حذف پریود در حالت اپ باز (سناریوی تازه رفع‌شده) — با دکمه «ارسال اعلان تستی» موجود در `ProfileView.tsx` برای تست سریع بدون صبر تا فردا.

## تحویل قبلی: جابه‌جایی دکمه‌ی «اعلان تستی» به بخش عیب‌یابی

کاربر بعد از تست موفق روی گوشی واقعی، خواست دکمه‌ی «ارسال یک اعلان تستی» را حذف نکنم (چون برای عیب‌یابی‌های آینده مفید است) ولی کم‌دیده‌تر بشود. الان از داخل بخش «یادآوری‌ها» (که همیشه باز بود) به یک `<details>` جدید و بسته‌به‌طور‌پیش‌فرض به اسم «عیب‌یابی اعلان‌ها» منتقل شد، درست قبل از دکمه‌ی «ذخیره تغییرات» در `ProfileView.tsx`. هیچ منطق دیگری تغییر نکرد.

## تحویل قبلی: بازطراحی اعلان‌ها بر اساس یک برنامه‌ی مرجع واقعاً کار کن (daroto)

کاربر یک برنامه‌ی خواهر (داروتو/appdaro — یادآور مصرف دارو) را به‌عنوان نمونه‌ی «نوتیفیکیشنش واقعاً کار می‌کند» ارائه داد و خواست همان حتمیت در رزا هم برقرار شود. با بررسی کدش (`NotificationEngine.ts` و `CapacitorNotificationAdapter.ts`)، سه تفاوت کلیدی با نسخه‌ی قبلی رزا پیدا و در `src/services/notificationService.ts` پیاده شد:

1. **هیچ‌وقت به `schedule.on{hour,minute}` + `repeats:true` اعتماد نکن.** برنامه‌ی مرجع برای هیچ‌کدام از اعلان‌هایش این الگو را استفاده نمی‌کند — همیشه هر رخداد را با یک `schedule.at`/تاریخ مطلق جداگانه می‌سازد. یادآوری‌های روزانه‌ی رزا (روتین صبح/شب، ثبت علائم) که در تحویل قبلی هنوز از `on+repeats` استفاده می‌کردند (که طبق تحقیق قبلی هم به‌شدت ناپایدار گزارش شده)، الان با تابع جدید `pushDailyRolling` به ۱۰ اعلانِ روزانه‌ی مجزا با تاریخ مطلق (برای ۱۰ روز آینده) تبدیل شدند — دقیقاً همان اصل. چون `scheduleRozaNotifications` با هر resume/تغییر تنظیمات دوباره اجرا می‌شود، این افق ۱۰‌روزه هر بار که کاربر اپ را باز می‌کند خودکار جلو کشیده می‌شود (رول‌آور).
2. **هر تماس با پلاگین باید timeout داشته باشد.** برنامه‌ی مرجع دقیقاً همین مشکل را داشته: اگر پل بومی (native bridge) هرگز جواب ندهد، بدون timeout کل زنجیره برای همیشه معلق می‌ماند و نه خطایی دیده می‌شود نه اعلانی می‌رسد. تابع `withTimeout` (سقف ۸ ثانیه، دقیقاً همان عدد برنامه‌ی مرجع) الان دور تمام فراخوانی‌های `LocalNotifications.*` پیچیده شده.
3. **مجوز هشدار دقیق را فقط گزارش نده، فعالانه حلش کن.** برنامه‌ی مرجع همان لحظه‌ی اول که کاربر اعلان را روشن می‌کند، اگر `checkExactNotificationSetting()` غیرفعال بود، بلافاصله `changeExactNotificationSetting()` را صدا می‌زند (کاربر را می‌برد به صفحه تنظیمات). رزا الان همین کار را می‌کند — ولی فقط دقیقاً همان لحظه‌ی اول grant شدن مجوز (نه هر resume روزمره، که مزاحم می‌شد).

فایل‌های تغییریافته/اضافه‌شده:
- `src/services/notificationService.ts` — `pushDailyRolling` (جایگزین کامل `on+repeats` برای سه اعلان روزانه)، `withTimeout` دور همه فراخوانی‌های پلاگین، منطق proactive-fix مجوز هشدار دقیق در همان لحظه‌ی اول grant، و `sendTestNotification()` جدید (اعلان واقعی ۵ ثانیه بعد — برای تست فوری بدون صبر تا فردا صبح، دقیقاً مثل دکمه‌ی تست برنامه‌ی مرجع).
- `src/components/profile/ProfileView.tsx` — دکمه‌ی «ارسال یک اعلان تستی» جدید زیر تنظیمات اعلان‌ها.
- `scripts/ensure-notification-permissions.mjs` (تغییر نام از `ensure-exact-alarm-permission.mjs`) — الان علاوه بر `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM`، مجوزهای `POST_NOTIFICATIONS` و `VIBRATE` را هم اضافه می‌کند — دقیقاً همان چهار مجوزی که در `AndroidManifest.xml` برنامه‌ی مرجع دیدم.
- `package.json` — رفرنس‌های اسکریپت به‌روزرسانی شد.

**تست انجام‌شده:** چون این محیط نه android/ واقعی دارد نه دستگاه، `scheduleRozaNotifications` را با یک پلاگین `@capacitor/local-notifications` جعلی (stub) در Node واقعاً اجرا کردم — تأیید شد که ۱۰ اعلان روزانه با تاریخ مطلق درست (روزهای متوالی، همان ساعت/دقیقه) برای هر کدام از سه یادآوری ساخته می‌شود، و منطق «اگر ساعت امروز گذشته، از فردا شروع کن» درست کار می‌کند (تست با ساعت ۰۰:۰۱ که قطعاً گذشته بود، تأیید شد افق از فردا شروع شد). `sendTestNotification` و `checkExactAlarmStatus` هم با stub تست و تأیید شدند.

**چیزی که نتوانستم تست کنم:** هنوز روی گوشی واقعی تست نشده — این محیط دستگاه/شبیه‌ساز ندارد. طبق توصیه تحویل قبلی، **حتماً روی یک گوشی اندروید واقعی (ترجیحاً اندروید ۱۲+) تست کن**، این‌بار هم با دکمه‌ی جدید «ارسال یک اعلان تستی» (که ظرف ۵ ثانیه جواب می‌دهد، نیازی به صبر تا فردا نیست) و هم با گذاشتن ساعت روتین چند دقیقه جلوتر.

## تحویل قبلی: رفع باگ اعلان‌های زمان‌بندی‌شده (روتین صبح/شب، ثبت علائم)

**علائم گزارش‌شده توسط کاربر:** «ساعتی رو تنظیم می‌کنم، اون لحظه فرا می‌رسه ولی نوتیفیکیشن ارسال نمی‌شه.»

**ریشه واقعی باگ (دو چیز، هر دو لازم بودند):**
1. `src/services/notificationService.ts` — یادآوری‌های «روتین صبح»، «روتین شب» و «ثبت علائم» (تنها سه موردی که با یک ساعت/دقیقه ثابت هر روز تکرار می‌شوند، نه تاریخ مطلق) با `schedule: { on: { hour, minute } }` ساخته می‌شدند **بدون فیلد `repeats: true`**. طبق رفتار مستندشده‌ی `@capacitor/local-notifications`، `repeats` پیش‌فرض `false` است و نبودش دقیقاً همین رفتار را توضیح می‌دهد. این را با جست‌وجوی وب و چند ایشوی باز روی `ionic-team/capacitor-plugins` (شماره‌های ۴۳۳۲، ۱۷۷۳، ۲۷۵۲) تأیید کردم — پیاده‌سازی اندرویدِ زمان‌بندی «on بدون تاریخ کامل» به‌شدت ناپایدار گزارش شده. راه‌حل: `repeats: true` صریح اضافه شد (دقیقاً همان الگویی که خودِ مستندات رسمی کاپاسیتور برای این حالت نشان می‌دهد).
2. حتی با `repeats: true` درست، از اندروید ۱۲ به بعد اعلان «دقیق» (exact) نیست مگر مجوز `SCHEDULE_EXACT_ALARM` در AndroidManifest.xml اعلام و توسط کاربر تأیید شده باشد؛ در غیر این صورت اعلان یا با تأخیر نامشخص می‌رسد یا اصلاً نمی‌رسد — بدون هیچ خطای قابل‌مشاهده‌ای برای کاربر یا توسعه‌دهنده.

**فایل‌های تغییریافته/اضافه‌شده برای این رفع باگ:**
- `src/services/notificationService.ts` — `repeats: true` به سه اعلان روزانه اضافه شد؛ دو تابع جدید صادر شد: `checkExactAlarmStatus()` (بررسی مجوز هشدار دقیق از طریق `LocalNotifications.checkExactNotificationSetting()`، با try/catch چون فقط روی اندروید و نسخه‌های جدید پلاگین موجود است) و `openExactAlarmSettings()` (باز کردن صفحه تنظیمات مربوطه از طریق `changeExactNotificationSetting()`). نوع `NotificationScheduleResult` یک مقدار جدید گرفت: `'exact-alarm-denied'`.
- `src/components/profile/ProfileView.tsx` — وقتی وضعیت `exact-alarm-denied` باشد، یک باکس هشدار زرد جدا از باکس قرمز «مجوز اعلان رد شده» نشان داده می‌شود، با دکمه‌ای که مستقیم `openExactAlarmSettings()` را صدا می‌زند.
- `scripts/ensure-exact-alarm-permission.mjs` — اسکریپت جدید و idempotent که مجوزهای `SCHEDULE_EXACT_ALARM` و `USE_EXACT_ALARM` را در `android/app/src/main/AndroidManifest.xml` تضمین می‌کند. **چون این پروژه هنوز پوشه‌ی `android/` ندارد (native project ساخته نشده)، این اسکریپت هنوز واقعاً روی یک مانیفست واقعی اجرا نشده — فقط با یک مانیفست ساختگی در محیط تست خودم چک شد و درست کار کرد (هم افزودن مجوز، هم idempotency، هم no-op وقتی android/ نیست).** بعد از اولین `npx cap add android`، حتماً `npm run cap:sync` را اجرا کن تا این اسکریپت واقعاً مجوزها را داخل مانیفست واقعی بنویسد؛ در `package.json` به `cap:sync` و به `android:build`/`android:release` وصل شده.

**چیزی که نمی‌توانستم اینجا تست کنم:** این محیط بدون `android/` و بدون دستگاه/شبیه‌ساز واقعی است، پس نمی‌توانم تأیید کنم که خودِ اعلان روی یک گوشی واقعی حالا سرِ وقت می‌رسد. منطق TS/JS را جدا با `tsc` + Node کامپایل و بررسی کردم (بدون خطای واقعی)، و اسکریپت مانیفست را با یک مانیفست ساختگی کامل تست کردم. **قبل از اعلام قطعیِ رفع باگ به کاربر نهایی، حتماً روی یک گوشی اندروید واقعی (ترجیحاً اندروید ۱۲+ که مجوز هشدار دقیق را دارد) تست کن**: تنظیم ساعت چند دقیقه جلوتر برای روتین صبح/شب، بستن اپ، و دیدن اینکه اعلان واقعاً می‌رسد.

## تحویل قبلی: جستجوی هوشمند (Smart Search)

قابلیت «جستجوی هوشمند» اضافه شد — یک دکمه ذره‌بین در هدر که یک مودال تمام‌صفحه باز می‌کند، کاربر عبارت محاوره‌ای یا کلیدواژه می‌نویسد و مستقیم به کارت/مودال واقعی همان ماده فعال، عارضه پوستی، مقاله یا موضوع راهنما وصل می‌شود. صددرصد سمت کلاینت، بدون سرور و بدون هیچ فراخوانی به مدل زبانی یا API خارجی — طبق محدودیت صریح کاربر.

فایل‌های جدید:
- `src/services/search/searchEngine.ts` — موتور جستجو. **از همان آرایه‌های دیتای موجود** (`INGREDIENTS_DATABASE`, `SKIN_CONDITIONS_DATABASE`, `GUIDE_TOPICS`, و مجموع سه منبع مقاله) در زمان اجرا ایندکس می‌سازد؛ هیچ فهرست دستی جدا ندارد. یعنی اضافه‌کردن یک آیتم جدید به هر کدام از آن فایل‌های دیتا، بدون هیچ تغییری در این فایل، خودبه‌خود در نتایج جستجو ظاهر می‌شود (این را با اضافه‌کردن موقت یک آیتم تست به `ingredients.ts` و اجرای `searchRoza(...)` تأیید کردم — نتیجه پیدا شد، بعد آیتم تست حذف شد و فایل به حالت اول برگشت).
  - نرمال‌سازی متن فارسی (یکسان‌سازی ی/ك، حذف اعراب، تبدیل ارقام) + توکنایز + امتیازدهی ساده (تطبیق کامل عبارت > تطبیق کلمه در عنوان > تطبیق زیررشته در عنوان > تطبیق در بدنه).
  - تشخیص سؤال «تداخل X با Y» و برگرداندن نتیجه واقعی از همان `checkPairConflict` که تداخل‌سنج اصلی استفاده می‌کند (نه چیز جدا).
  - اگر `SkinProfile` کاربر پاس داده شود، نتایج ماده فعال بر اساس `evaluateIngredientSafety` برچسب و اولویت‌بندی می‌شوند (بارداری/شیردهی/حساسیت و غیره).
- `src/components/common/SmartSearchModal.tsx` — UI جستجو، هم‌راستا با استایل بصری موجود (رنگ‌ها/فونت/spacing تکراری، چیزی از نو اختراع نشد).

فایل‌های تغییریافته برای دیپ‌لینک (تا نتیجه جستجو کاربر را مستقیم به همان کارت موجود ببرد، نه محتوای تکراری):
- `src/components/lab/SkinLab.tsx` — پراپ‌های جدید `initialIngredientId` و `initialConflictPair` (برای باز کردن مستقیم کارت یک ماده یا تب تداخل‌سنج با دو ماده از پیش پرشده).
- `src/components/knowledge/KnowledgeCenter.tsx` — پراپ‌های جدید `initialArticleId` و `initialConditionId` (باز کردن مستقیم مودال یک مقاله، یا تب عوارض پوستی با اسکرول و هایلایت روی همان عارضه).
- `src/components/layout/Header.tsx` — دکمه ذره‌بین جدید کنار زنگوله اعلان‌ها.
- `src/App.tsx` — state و `handleSearchResultSelect` که نتیجه جستجو را بر اساس `type` به همان بخش مربوطه هدایت می‌کند.

غنی‌سازی دیتا (پیش‌نیاز کیفیت جستجو):
- `src/types.ts` — فیلد اختیاری `commonNamesFa?: string[]` به `Ingredient` اضافه شد (فقط برای جستجو، در UI رسمی نمایش داده نمی‌شود).
- `src/services/content/ingredients.ts` — به هر ۱۵ ماده، `commonNamesFa` با نام‌های رایج/غلط‌نویسی/معادل انگلیسی اضافه شد.
- `src/services/content/conditions.ts` — به `symptomsFa` هر ۲۰ عارضه، ۲ عبارت محاوره‌ای واقعی‌تر اضافه شد (علائم رسمی قبلی دست‌نخورده ماندند).

نکات مهم برای ادامه کار:
- این محیط به اینترنت دسترسی نداشت (نه `npm ci`، نه دانلود پکیج)، پس فقط `searchEngine.ts` با `tsc` مستقل کامپایل و با Node تست شد (خروجی‌ها درست بودند). برای فایل‌های React، به‌خاطر نبود `node_modules`/`@types/react`، تایپ‌چک کامل ممکن نشد؛ فقط خطاهای واقعی نحوی/منطقی (نه خطاهای «ماژول پیدا نشد» ناشی از نبود node_modules) فیلتر و بررسی شدند و چیزی پیدا نشد. **قبل از هر build واقعی حتماً `npm ci && npm run lint` را اجرا کن** تا مطمئن شوی.
- محتوای جدید (مواد فعال بیشتر، عارضه بیشتر، مقاله بیشتر) طبق بند اختیاری پرامپت کاربر عمداً اضافه نشد تا تمرکز روی کیفیت خود موتور جستجو و دیپ‌لینک بماند؛ اگر کاربر خواست، همان الگوی `commonNamesFa` و ساختار دیتای فعلی برای موارد جدید هم به‌طور خودکار در جستجو ظاهر می‌شود.

## این تحویل چه چیزی عوض شد؟ (نسخه قبلی)

۱. **ترفندهای آرایش** (`src/components/makeup/MakeupTipsView.tsx`)
   - از ۱۰ به ۲۰ مطلب رسید (آیتم‌های ۱۱ تا ۲۰ اضافه شدند).
   - عکس آفلاین جدید برای هرکدام: `public/assets/makeup_photos/tip-11-contour.png` تا `tip-20-lash-care.png`.
   - یک نوار فیلتر (جستجو + دسته‌بندی، دقیقاً هم‌ساختار با فیلتر بخش «مقالات») اضافه شد.

۲. **مقالات (پایگاه دانش)** (`src/services/content/trendArticles.ts`)
   - از ۲۲ به ۳۲ مقاله رسید (شناسه‌های `art_trend_23` تا `art_trend_32`).
   - ۵ مقاله جدید به دسته «سلامت مو» (`cat_hair`) اضافه شد (روغن‌درمانی، شامپوی ضدریزش، آسیب حرارتی، کراتین/صافی، کمبود آهن).
   - ۵ مقاله دیگر به دسته‌های روتین، ترکیبات، هورمون، ضدآفتاب و سبک زندگی اضافه شد.
   - عکس آفلاین جدید برای هرکدام: `public/assets/real/articles_new/art_trend_23.jpg` تا `art_trend_32.jpg`.
   - فیلتر بخش مقالات (`src/components/knowledge/KnowledgeCenter.tsx`) از قبل جستجو + تب دسته‌بندی داشت؛ چیزی در آن تغییر نکرد، فقط محتوای جدید از همان مکانیزم عبور می‌کند.

## درباره عکس‌های موقت (placeholder)

همه عکس‌های جدید بالا، **جای‌گیر آفلاین** هستند (نه عکس واقعی محصول/مدل). با یک اسکریپت داخلی (HTML→تصویر، بدون نیاز به اینترنت) ساخته شدند تا:
- متن فارسی رویشان درست و خوانا باشد (نه بهم‌ریخته)،
- هرکدام مشخص کند مال کدام آیتم است (شماره + عنوان روی خودش نوشته شده).

برای جایگزینی با عکس نهایی: فقط فایل PNG/JPG را با همان نام و همان مسیر عوض کن؛ کد نیازی به تغییر ندارد. اگر اسم فایل عوض شد، فیلد `image` (در MakeupTipsView) یا `imageUrl` (در trendArticles) همان آیتم را هم به‌روز کن.

## نکات فنی برای ادامه کار

- این محیط به اینترنت دسترسی نداشت، پس `npm ci` اجرا/تست نشد. قبل از هر build واقعی حتماً `npm ci && npm run lint` را اجرا کن.
- تمام محتوای متنی مقالات و ترفندها، متن اورجینال نوشته‌شده به سبک محتوای موجود پروژه است (نه کپی از یک سایت خاص) — این با قانون «هیچ عدد جعلی» و لحن غیرتشخیصی/غیرنسخه‌ای پروژه هم‌خوان است (نگاه کن به `README.md`، بخش «مرز پزشکی»).
- ساختار داده مقالات تغییر نکرد (`Article` در `src/types.ts`)؛ فقط رکورد جدید اضافه شد.

## یادداشت‌های قبلی (از نسخه‌های قبل پروژه)

برای سابقه کامل پروژه (فروشگاه، بک‌اند حساب کاربری، چرخه ماهانه و غیره)، فایل‌های حافظه Claude با کلید `roza-shop` را ببین — کاربر خواسته این یادداشت‌ها بین نسخه‌ها منتقل شوند.
